# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB (Preview) and CockroachDB (Preview).
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# Defaults to the local database deployed from the `docker-compose.yml` stack

DATABASE_URL="mysql://root@127.0.0.1:3306/turborepo"

# Supabase Configuration
# Generate a secure password for Postgres
POSTGRES_PASSWORD="your-secure-postgres-password"

# Generate a random string of at least 32 characters for JWT
JWT_SECRET="your-jwt-secret-at-least-32-chars-long"

# Generate these keys using Supabase CLI or dashboard
ANON_KEY="your-anon-key"
SERVICE_ROLE_KEY="your-service-role-key"

# Supabase URLs
SUPABASE_URL="http://localhost:8000"
SUPABASE_PUBLIC_URL="http://localhost:8000"

# Studio/Dashboard Access
DASHBOARD_USERNAME="your-admin-username"
DASHBOARD_PASSWORD="your-secure-admin-password"
